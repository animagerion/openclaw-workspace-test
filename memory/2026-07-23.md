# 2026-07-23

## Comprobación post-actualización OpenClaw + Gateway + npm

### Contexto
Paduel reportó problemas tras actualizar OpenClaw, gateway y npm. Pidió comprobación.

### Estado general
- OpenClaw app 2026.7.1-2 funcionando
- Node v24.18.0, npm 11.16.0
- Gateway local reachable (88ms), pid 2238745 activo
- Telegram: SETUP (no ERROR, solo "unavailable" en fast mode)
- Memoria activa
- Crons ok: Gmail check, auto-commit (commit c791ee1 hace 1 min), Santos Diarios

### Problemas detectados
1. **Disco crítico al 95%** (34G usados / 38G, 2.0G libres)
2. **3 crons en error:**
   - `Recordatorio asignatura` (tutor) — 15x error
   - `Agenda diaria 8h` — 15x error
   - `Weekly Memory Review` — 2x error
   - Decidimos mirar los crons en otro momento, foco en disco

## Limpieza de disco — Nivel 1

### Ejecutado (12:59 UTC)
| Item | Tamaño | Acción |
|---|---|---|
| `~/.npm` cache | 1.1G → 295M | `npm cache clean --force` |
| `/tmp/gogcli.tar.gz` | 8.4M | Borrado (duplicado) |
| `/tmp/gogcli_0.12.0_linux_arm64.tar.gz` | 8.4M | Borrado (duplicado) |
| `/tmp/obscura/` | 1.5G | Borrado (proyecto abril, sin uso) |
| `/tmp/obscura-x86_64-linux.tar.gz` | 26M | Borrado |
| `/tmp/calameo_montilla/` | 358M | Borrado (scraping Calameo marzo) |
| `/tmp/calameo_montilla_v2/` | 181M | Borrado |
| `/tmp/calameo_capture.log` | pequeño | Borrado |

**Total liberado: ~3.1G**
**Disco: 95% → 87%** (2.0G → 4.7G libres)

### Pendiente original
- Nivel 2: auditar `~/.cache` (5.2G) y `/home/gerion` (22G)
- Nivel 3: docker (sin acceso al socket), journald, snap

## Limpieza de caché — Nivel 2 (13:00-13:05 UTC)

### Auditoría `~/.cache/` (5.2G)
| Carpeta | Tamaño | Estado |
|---|---|---|
| whisper/ | 2.1G | MANTENER — skill `openai-whisper` activa (dictados Alonso) |
| camoufox/ | 1.3G | BORRADO — sin skill formal, plugin sin tocar desde mayo |
| ms-playwright/ | 942M | MANTENER — usado por `scripts/render_deck_pdf.js` |
| puppeteer/ | 626M | BORRADO — sin uso activo (Playwright lo cubre) |
| uv/ | 77M | MANTENER — runtime Python |
| node-gyp/ | 67M | BORRADO (auto-borrable) |
| Homebrew/ | 75M | BORRADO (raro en Linux, residual) |
| pip/ | 6.6M | BORRADO (auto-borrable) |
| fontconfig/ | 112K | BORRADO (auto-borrable) |
| mesa_shader_cache/ | 1.3M | BORRADO (auto-borrable) |
| matplotlib/ | 32K | BORRADO (auto-borrable) |
| py-yfinance/ | 48K | BORRADO (auto-borrable) |
| motd.legal-displayed | 0 | BORRADO |

### Resultado acumulado sesión completa
- **Disco: 95% → 82%** (de 2.0G libres a 6.8G libres)
- **Total liberado: ~5G** entre Nivel 1 + caché

### Decisión sobre Camofox
- Proyecto `~/.openclaw/workspace/camofox-browser/` INTACTO (código fuente + plugin OpenClaw)
- Solo borrada `~/.cache/camoufox/` (binarios runtime)
- Regenerable con `npm install` si algún día se reactiva la skill
- Plugin OpenClaw registrado sigue ahí, no desinstalado — solo limpia caché, no el plugin

### Pendiente
- Nivel 3 si hace falta: docker (sin acceso al socket), journald, snap
- Mirar 3 crons en error: Recordatorio asignatura, Agenda diaria 8h, Weekly Memory Review

### Meta-aprendizaje
- Hubo confusión con comandos duplicados del shell que parecían re-ejecuciones pero ya habían terminado. Resuelto con la verdad sobre el estado real sin repetir operaciones.
- Filosofía "data plumbing" aplicada: el problema (disco lleno) requiere auditar caché/datos antes que tocar el código.

### Notas
- Patrón recurrente: "disco al 95% reventó Docker Camofox" — seguir vigilando
- Sigue la filosofía Andrew Ng: el problema suele estar en los datos/plumbing, no en el modelo

## Lo que queda en `/tmp` (no tocado)
- `node_modules/` (130M) — scripts globales
- `node-compile-cache/` (59M)
- `jiti/` (52M) — runtime openclaw
- `gog/` (23M) — binario gogcli extraído
- Trabajos recientes: semana_santa_utrera_2026.pdf, masterclass_hedgefunds.mp3, news_bulletin_radio.mp3
